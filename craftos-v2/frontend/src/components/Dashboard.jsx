import React from "react";
export default function Dashboard() {
  return (
    <div>
      <h1>🌿 Tableau de bord</h1>
      <p>Bienvenue dans CraftOS v2 — interface Zen Mousse (React).</p>
      <p>Prototype : Clients (CRUD minimal) relié à l'API /api/clients.</p>
    </div>
  );
}
